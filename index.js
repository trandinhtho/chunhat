// Chào mừng thông báo
console.log("Xin chào, đây là một ví dụ về JavaScript!");

// Hàm tính tổng của hai số
function tinhTong(a, b) {
    return a + b;
}

// Sử dụng hàm tính tổng
var so1 = 5;
var so2 = 3;
var tong = tinhTong(so1, so2);
console.log("Tổng của", so1, "và", so2, "là:", tong);
